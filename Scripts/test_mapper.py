import json
import sys

count = 0
array_of_tweets = []

for line in sys.stdin:
    for line in sys.stdin:
        while line not '\n' or EOF:
            tweet += line
            array_of_tweets[count].append(tweet)
    count += 1

# with open("test_set.txt", "r") as f:
#     df = f.read().split('\n')
#     json_df = json.loads(df[0])
#     print(json_df['user'])
    # test1  = Test(f)
    # print
